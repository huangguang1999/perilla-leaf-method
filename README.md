# 紫苏叶方法 · Perilla-Leaf Method

> 走进顶级寿司店，所有人盯着的是金枪鱼大腹、海胆、鱼子酱——昂贵、显眼、性感。
> 但真正让一整套料理成立的，可能是一片来自特定产地的小小紫苏叶：不贵、不起眼，却去腥、提香、定调。
> **没有金枪鱼，菜单少一道菜；没有紫苏叶，整套餐端不上来。**

一个判断**任何事物**的 Claude Agent Skill —— 一件事的好坏、一个物品的优劣、一个决策做不做、两个选项哪个更好。
**别只盯显眼又贵的「金枪鱼」，往下拆到那片「没它整套就端不上来」的紫苏叶**（决定性、不可替代的底层环节）；**从底层事实出发，不从叙事出发**；一句话压舱：**「这件事撞到墙的时候，墙在哪里？」**

> 方法蒸馏自 X 投资人 Serenity（[@aleabitoreddit](https://x.com/aleabitoreddit)）的推理方式，由 [@yijiangren](https://x.com/yijiangren/status/2060572559863935050) 总结命名为「紫苏叶理论」，**已通用化、去除其原本投资领域细节**。NFA / 仅判断参考，最终自己拿主意。

---

## 安装

**最快（推荐，单 skill 最通用，跨 Claude Code / Cursor / Codex 等 50+ agent）：**
```bash
npx skills add huangguang1999/perilla-leaf-method
# 全局装到 ~/.claude/skills：加 -g ；只装到某 agent：-a claude-code
```

**手动（git clone → 复制/软链）：**
```bash
git clone https://github.com/huangguang1999/perilla-leaf-method
cp -r perilla-leaf-method ~/.claude/skills/        # 个人级（所有项目可用）
# 或放进 <你的项目>/.claude/skills/                  # 项目级（随仓库共享给协作者）
```

装好后**新开一个会话**即自动识别。直接说：「**用紫苏叶方法判断一下 X**」「这个**值不值 / 好不好 / A 还是 B**」「帮我想清楚**要不要 X**」「**出张思考路径图**」。

---

## 它产出什么

给任意一个要判断的对象，输出：
1. 一条 **「因为[证据] → 所以[推断]」** 链，串到结论；
2. 结论 = **倾向 + 把握（大/中/小）+ 一句话**；
3. 一张**极简「思考路径图」**（HTML 成品图，可截图）。

让别人（和未来的你）能**顺着证据复核每一步**，而不是只看到一个态度。

## 内核 8 条

1. **只对证据回应**（从底层出发，不从叙事出发）—— 先剥掉情绪 / 最响的名字 / 最性感的叙事。
2. **证据→推断链** —— 每步「因为[事实] → 所以[推断]」，永远给路径不只给结论。
3. **找那片紫苏叶（决定性因素）** —— 紫苏叶三问：① 非有不可？② 可替代？③ 压力放大谁最先崩？看谁最窄，不是看谁最大。
4. **剔除金枪鱼（重要 ≠ 决定性）** —— 显眼但换掉也不翻盘的，主动排除。
5. **反驳对立面** —— 论最强的相反结论（可让 AI 攻击自己）；钉不死的留作风险。
6. **随证据修正** —— 证据变了就改判断。
7. **不必等 100% 确定** —— 用「倾向 + 把握」表达。
8. **诚实标注** —— ✅亲眼/查到 · ⚠️听说/待核实 · 💭我猜 · ❓不知道；不编。

完整方法见 **[`SKILL.md`](./SKILL.md)**；展开清单 + 由来隐喻 + 完整案例 + 误区见 **[`reference.md`](./reference.md)**。

## 怎么用

- **对话触发**：装好后说「用紫苏叶方法判断一下 X」「出张思考路径图」即可。
- **手动按方法走**：照 `SKILL.md` 的 6 步（明确目标 → 列证据(标来源) → 逐条证据→推断 → 标紫苏叶+剔金枪鱼 → 反方一遍 → 汇成结论）。
- **出思考路径图**：复用 [`templates/diagram-template.html`](./templates/diagram-template.html)（纵向单脊柱、笔记风、细体斜体序号），把每步「证据 / 所以」和结论填进去；参考 `examples/`。生成的图默认输出到你 home 下的 `~/perilla-leaf-diagrams/`，浏览器打开即可看 / 截图。

## 结构

```
perilla-leaf-method/
├── SKILL.md                 # 方法论正文（= Claude skill 主体，agent 触发时读）
├── reference.md             # 由来隐喻 + 内核展开 + 2 个完整案例 + 误区
├── templates/
│   └── diagram-template.html  # 思考路径图模板（浏览器打开即成品图）
├── examples/                # 两个填好的真实案例图（判断一件事 / 一个标的）
├── README.md                # 本文件
└── LICENSE                  # MIT
```

## 思考路径图长什么样

浅色笔记风、一条单脊柱、每步「证据」卡 + ↓ + 「所以」卡、左侧细体斜体序号、底部结论框（倾向用颜色体现）。**只放 证据→推断 步骤 + 结论**，不堆别的。直接 `open examples/*.html` 看。

---

MIT © 2026 huangguang1999 · 教育性方法论参考，非投资/专业建议（NFA）。
